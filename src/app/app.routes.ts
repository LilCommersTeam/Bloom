import { Routes } from '@angular/router';
import {SignUpFirstStepComponent} from "./Components/Sign-up/sign-up.component";

export const routes: Routes = [
  { path: 'Log-in', component: SignUpFirstStepComponent },
];
